## Site Menu

The main site menu dropdowns are not accessible

## TABS

Tabs are <a> links with no href with a <span> inside. They should be <buttons> with proper tab roles, aria-labels etc
The content below should have proper roles too

## Tables

Need to get rid of nested <tables> within <tables>
Pagination uses <a> with onClick so they're not keyboard selectable
